name: 'dbt_acidentes'
version: '1.0'
config-version: 2

profile: 'default'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_modules"]

models:
  dbt_acidentes:
    +materialized: view
    staging:
      +materialized: view
    mart:
      +materialized: table
